# Day 11

Problem: Sort Characters By Frequency

leetcode link:

```js

```

Input: string

Output: string, modified based on algorithm

Algorithm:
We are given a string, and we have to sort it in decreasing order,
based on how frequent the characters are used
Note that uppercase and lowercase characters are treated as two different characters, so a "a" and a "A" won't count towards the same frequency
Also note, you don't need to organize letters alphabetically, just frequency wise

Python Solution:

```py
# solution here

```
